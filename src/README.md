# src

Place application code here.

Until the project direction is known:

- prefer small, explicit modules
- organize by feature or domain when possible
- avoid deep framework-specific structure unless needed
